# CRM Web (SPA Shell)

`crm_web` is a minimal single-page shell for Evolve Sprouts.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The static output is generated in `out/`.
