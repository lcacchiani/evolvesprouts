title = "Evolve Sprouts gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allow non-secret media resource taxonomy keys"
regexTarget = "line"
regexes = [
  '''\b4-ways-patience\b''',
  '''"ApiCustomDomainName"\s*:\s*"[^"]*"''',
]
