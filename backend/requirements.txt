# Core dependencies with pinned versions for reproducible builds
# Update versions carefully - test thoroughly before deploying

# Database ORM
SQLAlchemy==2.0.46

# AWS SDK
boto3==1.42.36

# PostgreSQL adapter (psycopg3)
psycopg[binary]==3.3.2

# Development dependencies (optional, uncomment for local development)
# pytest>=8.0.0,<9.0.0
# pytest-cov>=4.1.0,<5.0.0
# ruff>=0.3.0,<1.0.0
# mypy>=1.8.0,<2.0.0
